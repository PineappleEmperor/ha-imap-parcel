name: PR Label & Manifest Check

on:
  workflow_run:
    workflows: ["Create/Update Dev PR"]
    types: [completed]
  pull_request:
    types: [opened, reopened, synchronize, edited]

permissions:
  contents: read
  pull-requests: write

jobs:
  update-title:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.event.action != 'edited'
    steps:
      - name: Set PR title from highest semantic commit
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BEST=""
          PRIORITY=0

          while IFS= read -r subject; do
            if echo "$subject" | grep -qiE '^(feat|fix|chore|docs|refactor|perf|test|build|ci|style|revert)(\(.+\))?!:'; then
              if [ $PRIORITY -lt 4 ]; then PRIORITY=4; BEST="$subject"; fi
            elif echo "$subject" | grep -qiE '^feat(\(.+\))?:'; then
              if [ $PRIORITY -lt 3 ]; then PRIORITY=3; BEST="$subject"; fi
            elif echo "$subject" | grep -qiE '^fix(\(.+\))?:'; then
              if [ $PRIORITY -lt 2 ]; then PRIORITY=2; BEST="$subject"; fi
            elif echo "$subject" | grep -qiE '^(chore|docs|refactor|perf|test|build|ci|style|revert)(\(.+\))?:'; then
              if [ $PRIORITY -lt 1 ]; then PRIORITY=1; BEST="$subject"; fi
            fi
          done < <(gh api repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/commits --jq '.[].commit.message | split("\n")[0]')

          if [ -n "$BEST" ]; then
            gh pr edit ${{ github.event.pull_request.number }} --title "$BEST" --repo ${{ github.repository }}
          else
            echo "::error::No conventional commit found (feat:, fix:, chore:, etc.). Add a semantic prefix to at least one commit."
            exit 1
          fi

  label:
    runs-on: ubuntu-latest
    needs: update-title
    if: always() && github.event_name == 'pull_request' && (needs.update-title.result == 'success' || needs.update-title.result == 'skipped')
    steps:
      - name: Apply label from PR title
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TITLE=$(gh pr view ${{ github.event.pull_request.number }} --json title -q '.title' --repo ${{ github.repository }})

          if echo "$TITLE" | grep -qiE '^(feat|fix|chore|docs|refactor|perf|test|build|ci|style|revert)(\(.+\))?!:'; then
            LABEL="!feature"
          elif echo "$TITLE" | grep -qiE '^feat(\(.+\))?:'; then
            LABEL="feature"
          elif echo "$TITLE" | grep -qiE '^fix(\(.+\))?:'; then
            LABEL="fix"
          elif echo "$TITLE" | grep -qiE '^(chore|docs|refactor|perf|test|build|ci|style|revert)(\(.+\))?:'; then
            LABEL="chore"
          else
            echo "No matching label for: $TITLE"
            exit 0
          fi

          gh pr edit ${{ github.event.pull_request.number }} --add-label "$LABEL" --repo ${{ github.repository }}

  check-version:
    name: Ensure version is updated in manifest.json
    runs-on: ubuntu-latest
    needs: label
    if: always() && (needs.label.result == 'success' || github.event_name == 'workflow_run')
    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_sha || github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Resolve PR context
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            BRANCH="${{ github.event.workflow_run.head_branch }}"
            PR_NUMBER=$(gh pr list --head "$BRANCH" --base main --state open --json number --jq '.[0].number // empty')
            BASE_REF=main
          else
            PR_NUMBER="${{ github.event.pull_request.number }}"
            BASE_REF="${{ github.event.pull_request.base.ref }}"
          fi
          echo "PR_NUMBER=$PR_NUMBER" >> $GITHUB_ENV
          echo "BASE_REF=$BASE_REF" >> $GITHUB_ENV

      - name: Checkout base manifest.json
        run: |
          git fetch origin $BASE_REF
          git show origin/$BASE_REF:custom_components/ha_imap_parcel/manifest.json > ../manifest_base.json 2>/dev/null \
            || echo '{"version": "0.0.0"}' > ../manifest_base.json

      - name: Compare manifest versions
        id: compare
        run: |
          BASE_VERSION=$(jq -r '.version' ../manifest_base.json)
          PR_VERSION=$(jq -r '.version' custom_components/ha_imap_parcel/manifest.json)

          echo "Base version: $BASE_VERSION"
          echo "PR version: $PR_VERSION"
          echo "base_version=$BASE_VERSION" >> $GITHUB_OUTPUT
          echo "pr_version=$PR_VERSION" >> $GITHUB_OUTPUT

          if [ "$BASE_VERSION" == "$PR_VERSION" ]; then
            echo "unchanged=true" >> $GITHUB_OUTPUT
          else
            echo "unchanged=false" >> $GITHUB_OUTPUT
          fi

      - name: Determine bump type from PR labels
        id: bump
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          [ -z "$PR_NUMBER" ] && exit 0

          LABELS=$(gh pr view "$PR_NUMBER" --json labels -q '.labels[].name')

          BASE_VERSION="${{ steps.compare.outputs.base_version }}"
          echo "Base version: $BASE_VERSION"

          if [[ "$BASE_VERSION" =~ ^([0-9]+)\.([0-9]+)\.([0-9]+)$ ]]; then
            MAJOR="${BASH_REMATCH[1]}"
            MINOR="${BASH_REMATCH[2]}"
            PATCH="${BASH_REMATCH[3]}"
          else
            echo "❌ Could not parse version number: $BASE_VERSION"
            exit 1
          fi

          if echo "$LABELS" | grep -qiE '!feat|!feature|major'; then
            BUMP="xfeature"
          elif echo "$LABELS" | grep -qiE 'feat|feature'; then
            BUMP="feature"
          elif echo "$LABELS" | grep -qiE 'fix|patch|chore'; then
            BUMP="patch"
          else
            BUMP="none"
          fi

          if [ "$BUMP" = "xfeature" ]; then
            EXPECTED_VERSION="v$((MAJOR+1)).0.0"
          elif [ "$BUMP" = "feature" ]; then
            EXPECTED_VERSION="v$MAJOR.$((MINOR+1)).0"
          elif [ "$BUMP" = "patch" ]; then
            EXPECTED_VERSION="v$MAJOR.$MINOR.$((PATCH+1))"
          else
            EXPECTED_VERSION=""
          fi

          echo "RECOMMENDED_BUMP=$BUMP" >> $GITHUB_OUTPUT
          echo "SUGGESTED_VERSION=$EXPECTED_VERSION" >> $GITHUB_OUTPUT

      - name: Comment on PR if version unchanged
        if: steps.compare.outputs.unchanged == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          [ -z "$PR_NUMBER" ] && exit 0

          BUMP_TYPE="${{ steps.bump.outputs.RECOMMENDED_BUMP }}"
          NEW_VERSION="${{ steps.bump.outputs.SUGGESTED_VERSION }}"
          MSG="<!-- version-check -->"
          MSG+="⚠️ **Version in \`custom_components/ha_imap_parcel/manifest.json\` has not been updated.**

          Please update the \`version\` field according to [Semantic Versioning](https://semver.org/)."

          if [ "$BUMP_TYPE" != "none" ]; then
            MSG+="

          🔍 Based on the PR labels, a **$BUMP_TYPE** version bump is recommended.

          💡 Suggested new version: \`$NEW_VERSION\`"
          else
            MSG+="

          💡 No \`breaking feature\`, \`feature\`, or \`fix\` label was found — please add one to indicate the type of change."
          fi

          COMMENT_ID=$(gh api repos/${{ github.repository }}/issues/$PR_NUMBER/comments \
            --jq '.[] | select(.body | contains("<!-- version-check -->")) | select(.user.type == "Bot") | .id')

          if [ -n "$COMMENT_ID" ]; then
            gh api --method PATCH -H "Accept: application/vnd.github+json" \
              /repos/${{ github.repository }}/issues/comments/$COMMENT_ID \
              -f body="$MSG"
          else
            gh pr comment "$PR_NUMBER" --body "$MSG"
          fi

          exit 1

      - name: Comment if version number is incorrect
        if: >
          steps.compare.outputs.unchanged == 'false' &&
          steps.bump.outputs.SUGGESTED_VERSION != '' &&
          steps.bump.outputs.SUGGESTED_VERSION != format('v{0}', steps.compare.outputs.pr_version)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          [ -z "$PR_NUMBER" ] && exit 0

          BUMP_TYPE="${{ steps.bump.outputs.RECOMMENDED_BUMP }}"
          NEW_VERSION="${{ steps.bump.outputs.SUGGESTED_VERSION }}"
          PR_VERSION="${{ steps.compare.outputs.pr_version }}"
          MSG="<!-- version-check -->"
          MSG+="⚠️ **Version in \`custom_components/ha_imap_parcel/manifest.json\` seems incorrect.**

          Please update the \`version\` field according to [Semantic Versioning](https://semver.org/)."

          if [ "$BUMP_TYPE" != "none" ]; then
            MSG+="

          🔍 Based on the PR labels, a **$BUMP_TYPE** version bump is recommended.

          💡 Suggested new version: \`$NEW_VERSION\`"
          else
            MSG+="

          💡 No \`breaking feature\`, \`feature\`, or \`fix\` label was found — please add one to indicate the type of change."
          fi

          COMMENT_ID=$(gh api repos/${{ github.repository }}/issues/$PR_NUMBER/comments \
            --jq '.[] | select(.body | contains("<!-- version-check -->")) | select(.user.type == "Bot") | .id')

          if [ -n "$COMMENT_ID" ]; then
            gh api --method PATCH -H "Accept: application/vnd.github+json" \
              /repos/${{ github.repository }}/issues/comments/$COMMENT_ID \
              -f body="$MSG"
          else
            gh pr comment "$PR_NUMBER" --body "$MSG"
          fi

          exit 1
